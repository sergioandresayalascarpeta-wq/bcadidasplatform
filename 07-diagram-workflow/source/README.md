# Optional Mermaid snippets

Extract standalone `.mmd` snippets here if you want to sync them with Mermaid Chart or CI.

Most of the time the canonical blocks live in `01-architecture/*.md`.

See [`../diagram-workflow.md`](../diagram-workflow.md).
